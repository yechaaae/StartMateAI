# StartMate AI — 서비스 소개 (디자인 핸드오프용)

> 이 문서는 **StartMate AI를 처음 보는 디자이너/디자인 AI**가 "어떤 서비스이고, 누구를 위한 것이고, 어떤 화면과 흐름이 있는지"를 한 번에 이해하도록 만든 자체 완결형 설명서입니다. 코드/레포 지식 없이 이 문서만으로 디자인 작업을 시작할 수 있게 작성했습니다.

---

## 1. 한 줄 정의

**소상공인·예비창업자를 위한 AI 창업·경영 컨설턴트.**
사용자가 자연어로 질문하면, 역할이 다른 여러 AI 에이전트가 **함께 검토·토론**하고, 그 결과를 **기능별 리포트**로 정리해 줍니다.

> SSAFY × Kakao Tech Bootcamp AI 해커톤 출품작입니다.

엘리베이터 피치:
> "혼자 창업 준비하면 아이템 선정, 자금 시뮬레이션, 지원사업 찾기, 사업계획서, 운영 개선, 홍보까지 전부 막막합니다. StartMate AI는 이 6가지를 각 분야 전문 AI 에이전트가 맡아, 채팅 한 번으로 '진단 → 근거 → 다음 액션'까지 정리해 주는 창업 파트너입니다."

---

## 2. 누구를 위한 서비스인가 (타깃)

| 페르소나 | 상황 | 핵심 니즈 |
| --- | --- | --- |
| **예비창업자** | 아이템은 있는데 어디서 시작할지 모름 | 내 조건에 맞는 아이템·자금·지원사업 가이드 |
| **초기 소상공인** | 가게는 열었는데 운영/홍보가 막막 | 운영 지표 진단, 개선 제안, SNS 홍보 자동화 |
| **지원사업 준비자** | 정부 지원금 받고 싶은데 뭐가 있는지 모름 | 내 프로필에 맞는 지원사업 추천 + 사업계획서 |

대표 페르소나 예시(서비스 내 샘플): *20대, 시각디자인 전공, 카페 매니저 경력 1년, 초기자금 100만 원, 1인 창업, SNS 운영 경험 있음, 부산 거주.*

---

## 3. 핵심 가치 (디자인이 살려야 할 3가지 인상)

이 서비스의 "잘 만들어졌다"는 느낌은 아래 3가지에서 나옵니다. **디자인의 최우선 목표는 이 3가지를 시각적으로 또렷하게 만드는 것**입니다.

1. **여러 AI가 실제로 협업한다는 느낌**
   - 질문하면 "오케스트레이터가 필요한 에이전트를 고르고 → 각 에이전트가 검토 시작 → 완료"가 **실시간 진행 말풍선**으로 순차적으로 뜸.
2. **에이전트들이 서로 교차검증/토론한다는 느낌**
   - `검토 → 의견 충돌(challenge) → 보완(revision) → 합의(consensus)` 흐름을 보여줌.
3. **실제 공공데이터에 근거한다는 신뢰감**
   - 지원사업 매칭 점수, 상권 경쟁점 수/강도 같은 **숫자와 근거 카드**를 노출.

---

## 4. 6대 핵심 기능 (AI 기능 메뉴)

각 기능은 고유 색(아래 §8 토큰)과 담당 대표 에이전트를 가집니다. 각 기능 화면은 **왼쪽 리포트 + 오른쪽 채팅** 2단 구성입니다.

| # | 기능(메뉴명) | 한 줄 설명 | 대표 에이전트 | 리포트 형태 |
| --- | --- | --- | --- | --- |
| 1 | **아이템 추천** (item) | 프로필·예산에 맞는 창업 아이템 후보 랭킹 | 아이디어 | 순위 카드(점수/이유) + 상권 분석 |
| 2 | **창업 시뮬레이터** (simulator) | 30일 손익분기점·현금흐름 시뮬레이션 | 재무 | 일자별 차트 + 요약 지표 |
| 3 | **지원사업 추천** (support) | 내 조건에 맞는 정부 지원사업 매칭 | 정책 | 지원사업 리스트(적합도/마감/서류) |
| 4 | **사업계획서** (plan) | 지원사업 제출용 사업계획서 초안 | 기획 | 섹션별 아코디언 문서 |
| 5 | **운영 피드백** (operation) | 매출·주문·광고 등 운영 지표 진단 | 운영 | KPI 카드 + 상품 비중 바 + 개선 제안 |
| 6 | **SNS 홍보** (sns) | 홍보 글 자동 생성 + Threads 바로 게시 | 마케팅 | 캠페인 작성기 + 게시 기록 |

> 기능 간 연결: 예) 아이템 추천에서 고른 아이템 → 시뮬레이터/사업계획서/SNS로 맥락이 이어짐. 운영 피드백의 개선 제안 → SNS 홍보 초안으로 바로가기.

---

## 5. AI 멀티에이전트 (서비스의 심장)

내부적으로 **오케스트레이터 + 10개 전문 에이전트** 구조입니다. 디자인에서는 각 에이전트를 **아바타 + 고유색 + 역할 라벨**로 일관되게 표현합니다.

| 에이전트 | 역할 | 색 토큰 |
| --- | --- | --- |
| 매니저(오케스트레이터) | 질문 분석, 에이전트 선택, 최종 합성 | `--a-manager` #4f6bf0 |
| 프로필 | 사용자 조건 분석 | `--a-profile` #2f80d8 |
| 아이디어 | 아이템 후보 탐색 | `--a-idea` #7c5cf0 |
| 재무 | 비용·매출·손익 검토 | `--a-finance` #1f9d6b |
| 정책 | 지원사업 추천/판단 | `--a-policy` #e0922f |
| 기획 | 사업계획서 초안 | `--a-plan` #7c5cf0 |
| 운영 | 운영 리스크·개선 | `--a-operation` #0fa3a3 |
| 마케팅 | 홍보/SNS 전략 | `--a-marketing` #e25c9a |
| 상권 | 입지·경쟁점 분석 | `--a-area` #e2685c |
| 법률 | 인허가·체크리스트 | `--a-legal` #566489 |

**진행 이벤트(실시간 표시용) 종류** — 채팅 영역에서 말풍선/상태로 표현되어야 함:
`오케스트레이터 시작 → 에이전트 선택됨 → 에이전트별 검토 시작/완료 → (의견충돌 challenge → 보완 revision) → 합의 consensus → 합성 중 → 완료`

> 디자인 포인트: 이 진행 단계가 **너무 빨리 지나가지 않고, 하나씩 쌓이며 보이는** 연출이 핵심 매력입니다. 타이핑 인디케이터, 단계별 체크, 에이전트 아바타 점등 등이 어울립니다.

---

## 6. 사용자 여정 (화면 흐름)

```
랜딩 → 로그인/회원가입 → 온보딩(프로필 입력)
   → 홈(프로필 + 적합도 진단 + 기능 메뉴)
        ├─ 6대 기능 페이지 (리포트 + 채팅)
        ├─ AI와 논의하기 (자유 채팅, 멀티턴)
        └─ 저장한 결과 (리포트 보관함)
```

### 화면별 설명

| 화면 | 역할 | 디자인 메모 |
| --- | --- | --- |
| **랜딩** | 서비스 소개 + 로그인 유도 | 첫인상. "AI 창업 파트너" 브랜드 |
| **로그인/회원가입** | 이메일→비밀번호 순차 입력 | 단계적 필드 노출(한 번에 하나씩) |
| **온보딩** | 전공/경력/자금/팀/업종 등 프로필 3단계 입력 | 이후 모든 AI 판단의 기준 데이터 |
| **홈** | "입력한 프로필" 카드 + "AI 창업 적합도 진단"(점수 게이지) + 6기능 그리드 | 적합도 점수 원형 게이지가 시각 포인트 |
| **기능 페이지(6종)** | 좌: 리포트 패널 / 우: 채팅 | 2단 레이아웃. AI 생성/갱신/저장 버튼 |
| **AI와 논의하기** | 자유 주제 멀티턴 채팅 | 진행 말풍선·토론 연출이 가장 잘 보이는 곳 |
| **저장한 결과** | 생성한 리포트 보관·재열람 | 카드 리스트 |
| **사이드바** | 워크스페이스 전환 + 기능 네비 + 사용자 박스 | 항상 보이는 좌측 고정 |

---

## 7. 운영 피드백 리포트 (최근 리뉴얼, 디자인 기준 예시)

가장 최근에 정비된 화면이라 **다른 리포트의 디자인 기준점**으로 삼기 좋습니다. 구성:

1. **운영 진단 리포트 카드**
   - 상단: 제목 + 기간/요약
   - **KPI 4열 카드 그리드** — 각 카드: 라벨(작게) / 값(크게 굵게) / 증감 델타(상승=초록 `--ok`, 하락=빨강 `--bad`)
   - **상품별 판매 비중** — 이름·% + 가로 바(`--a-operation` 색 채움)
   - **채널 진단** — 채널명 + AI 진단 메모
   - **운영 메모** — 자유 텍스트
   - 하단: "이 운영 피드백 저장" 버튼(`--a-operation` 색)
2. **개선 제안 카드**
   - 제안 카드 리스트(제목+본문), 선택 시 하이라이트(`--a-operation-t` 배경)
   - 일부 제안은 "SNS 홍보 초안으로" 바로가기 버튼(`--a-marketing` 색)

---

## 8. 디자인 시스템 현황 (이미 정의된 토큰)

서비스에는 이미 디자인 토큰이 있습니다. **새로 만들지 말고 이 토큰 위에서 확장**해 주세요. (CSS 변수 기준)

### 기본 색
```
--bg:#f3f4f7        화면 배경(연회색)
--panel:#ffffff     카드/패널
--line:#ececf0      얇은 경계선
--line-2:#e3e4ea    입력 경계선
--ink:#1f2640       본문 텍스트(짙은 남색)
--ink-2:#5b6172     보조 텍스트
--ink-3:#9499a6     라벨/캡션
--ink-4:#b9bdc8     가장 옅은 텍스트
```

### 브랜드 / 상태색
```
--brand:#4f6bf0          메인 브랜드(블루)
--brand-strong:#3f5be0   강조
--brand-tint:#eaeefd     브랜드 옅은 배경
--ok:#1f9d6b   긍정/상승(초록)
--warn:#e0922f 주의(주황)
--bad:#e25c5c  부정/하락(빨강)
```

### 에이전트/기능 액센트색
§5 표 참고. 각 액센트는 진한색(`--a-xxx`)과 옅은 배경(`--a-xxx-t`) 페어로 존재.

### 형태/타이포
```
폰트: Pretendard (한국어 최적화 산세리프)
모서리: --r-sm 8px / --r 12px / --r-lg 16px / --r-xl 20px
그림자: 부드럽고 옅음 (--shadow), 강조 시 --shadow-lg
```

**전체 톤**: 밝은 배경 + 카드 기반, 둥근 모서리, 부드러운 그림자, 파스텔 액센트. 신뢰감 있되 친근하고 가벼운 SaaS 느낌.

---

## 9. 톤 & 보이스

- **말투**: 친근한 존댓말. "~해요", "~볼게요". 전문적이지만 어렵지 않게.
- **포지셔닝**: "확정/단정"이 아니라 "가능성/제안". 지원사업도 "지원 가능 확정"이 아닌 "가능성·주의사항"으로 표현.
- **에이전트 의인화**: 각 에이전트가 사람처럼 "검토 중이에요", "이 부분은 위험해 보여요"라고 말함 → 협업 느낌.

---

## 10. 현재 상태 (디자인이 알아야 할 현실)

해커톤 MVP라 일부는 실제 데이터, 일부는 목/시드 데이터로 동작합니다. 디자인 시 "이 화면이 실데이터냐"를 구분해야 하므로 명시합니다.

- **실데이터**: 로그인 사용자, 온보딩 프로필, 채팅/진행 이벤트, 기능 리포트 AI 결과(백엔드 가동 시)
- **목/시드**: 워크스페이스 목록(3개 고정), 지원사업 "검색" 결과(고정 4건), 리포트 초기 표시값, 일부 placeholder
- 백엔드 미가동 시 기능 리포트는 시드 값으로 보임

> 디자인 작업 자체에는 영향이 적지만, "이 숫자는 예시"라는 점을 알고 레이아웃을 잡으면 됩니다.

---

## 11. 디자인에 특히 도움받고 싶은 것 (요청 후보)

1. **진행/토론 연출 비주얼** — 에이전트 협업이 "살아있게" 보이는 인터랙션(말풍선 흐름, 단계 표시, 아바타 상태).
2. **리포트 카드 시스템 통일** — 6개 기능 리포트가 제각각이라, 공통 카드/타이포/간격 규칙 정립.
3. **홈 적합도 진단** — 점수 게이지 + 강점/리스크/추천 요약의 정보 위계.
4. **근거 카드(evidence)** — 지원사업 점수, 상권 경쟁점 수 같은 데이터를 신뢰감 있게 보여주는 컴포넌트.
5. **모바일 대응** — 현재 2단(리포트+채팅) 레이아웃의 좁은 화면 대응.

---

## 12. 한 장 요약 (TL;DR)

- **무엇**: 소상공인·예비창업자용 AI 창업 컨설턴트
- **어떻게**: 10개 전문 AI 에이전트가 협업·토론 → 6대 기능 리포트로 정리
- **핵심 매력**: ① 실시간 에이전트 협업 연출 ② 교차검증/토론 ③ 공공데이터 근거
- **화면**: 랜딩 → 로그인 → 온보딩 → 홈 → (기능 6종 / 자유 채팅 / 저장함), 좌측 사이드바 고정
- **비주얼**: Pretendard, 밝은 배경 + 둥근 카드 + 파스텔 에이전트 액센트, 친근한 존댓말
- **디자인 목표**: 협업·토론·데이터근거 3가지를 시각적으로 또렷하게
